{
  "name": "briefr-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "drizzle-kit": "^0.30.2",
    "turbo": "^2.4.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
